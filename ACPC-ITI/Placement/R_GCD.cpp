#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> vect(n);
    int q;
    cin >> q;
    while (q--)
    {
        bool flag = true;
        int val;
        cin >> val;
        for (int i = 0; i < n; i++)
        {
            if (v[i] % val != 0)
            {
            }
        }
    }
    return 0;
}
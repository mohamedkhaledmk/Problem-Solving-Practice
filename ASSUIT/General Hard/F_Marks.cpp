#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    vector<string> v(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
    int studs = 0;
    for (int i = 0; i < m; i++)
    {
        bool flg = false;
        for (int j = 0; j < n; j++)
        {
            for (int k = 0; k < m; k++)
            {
                        }
        }
        if (!flg)
            studs++;
    }
    cout << studs;

    return 0;
}